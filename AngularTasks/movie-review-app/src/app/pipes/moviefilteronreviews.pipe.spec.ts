import { MoviefilteronreviewsPipe } from './moviefilteronreviews.pipe';

describe('MoviefilteronreviewsPipe', () => {
  it('create an instance', () => {
    const pipe = new MoviefilteronreviewsPipe();
    expect(pipe).toBeTruthy();
  });
});
